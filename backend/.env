# Server Config
PORT=5000

# MongoDB URI (Local or MongoDB Atlas)
MONGODB_URI=mongodb://127.0.0.1:27017/ds_safety

# Admin Details (Values from catalogue)
ADMIN_EMAIL=dsengineering.py@gmail.com
ADMIN_WHATSAPP_NUMBER=+919944182596

# SMTP Email Config (Leave empty to trigger Console Logging fallback)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="DS Safety Enquiries <noreply@dsengineering.com>"

# Twilio WhatsApp Config (Leave empty to trigger Console Logging fallback)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=
